
1. Install all packages

    ```bash
    npm install
    ```

2. Run development server

    ```bash
    npm run dev
    ```

3. Generate build files for deployment

    ```bash
    npm run build
    ```
